{% extends 'base.html.twig' %}
{%  block body %}
    {% if (wish.isPublished) %}
        <h2>{{ wish.title }}</h2>
        <i>{{ wish.author }}</i>
        <p>{{ wish.description }}</p>
    {% endif %}
{% endblock %}